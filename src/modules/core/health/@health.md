# @health

Esta carpeta representa la feature de salud de la API.

## Estado actual

- `health.module.js`: une el controller y el service del chequeo de salud.
- `health-get/`: contiene la implementacion activa del endpoint.

## Nota importante

A diferencia de `about`, esta feature si toca base de datos para comprobar que el backend puede ejecutar queries.

# @info

Esta carpeta parece reservada para la feature `info`, probablemente relacionada con operaciones futuras sobre informacion del dominio.

## Estado actual

- Existen subcarpetas para `info-create`, `info-delete` e `info-get`.
- No hay `info.module.js`.
- `core.module.js` no registra esta feature, asi que hoy no expone endpoints publicos.

## Nota importante

Esta feature esta en preparacion. Conviene verla como trabajo pendiente, no como parte funcional del backend.

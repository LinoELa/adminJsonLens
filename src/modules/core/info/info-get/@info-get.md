# @info-get

Esta carpeta esta reservada para futuros endpoints de lectura de la feature `info`.

## Estado actual

La carpeta existe, pero no contiene archivos.

## Nota importante

Si se implementa, deberia seguir el mismo patron que `about-get/`, `health-get/` y `users-get/`.

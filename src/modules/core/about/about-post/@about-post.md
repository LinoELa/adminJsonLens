# @about-post

Esta carpeta esta reservada para una posible variante `POST` de la feature `about`.

## Estado actual

No tiene archivos implementados ni esta conectada en `about.module.js`.

## Nota importante

Si esta carpeta no se va a usar, conviene eliminarla. Si se usa, deberia seguir el mismo patron que `about-get/`.

# 20-03-2026

## Estado actual

- La jerarquia quedo asi: `index.module -> core.module + repository.module`.
- `core.module` agrupa `about`, `health` y `users`.
- Cada modulo importa su `controller` y su `service`.
- `repository.module` agrupa la parte comun del repository y la parte especifica de usuario.
- Las rutas `/about`, `/health` y `/users` responden bien.
- La funcion `prefixApi()` sigue devolviendo `/api`.

## Pendiente para luego

- Mejorar el manejo cuando falte configuracion de base de datos.
- Mover la creacion de la tabla `users` fuera de `GET /users`.
- Revisar el modulo `info` y alinearlo con la misma estructura modular.
- Limpiar carpetas viejas o vacias que ya no aportan nada.

## Nota

- La estructura de `repository.module` gusta y se puede mantener como base para los siguientes modulos.
